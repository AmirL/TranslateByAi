{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "name": "translate-by-ai",
  "scripts": {
    "start:dev": "lerna run start:dev --parallel --stream",
    "start:prod": "lerna run start:prod --parallel --stream",
    "start:nats-server": "./nats-docker-start.sh"
  },
  "devDependencies": {
    "lerna": "^6.6.2"
  }
}
